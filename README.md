# reflector

Reflects link-local service traffic — Wake-on-LAN, mDNS, SSDP, and an optional
DIAL proxy — between two network interfaces, bridging devices stranded on
different layer-2 segments.

## Status

Early scaffolding.
