# Pinned FreeBSD release for the publish pipeline's daemon-package build VMs:
# the FreeBSD BASE of the OPNsense series this major serves (the series is
# named by the publish workflows' matrices), NOT the newest 14.x point
# release. pkg stamps every package it creates with the build host's
# __FreeBSD_version, and pkg on the target rejects anything newer than its
# own userland, so the daemon must be built on the base its OPNsense target
# runs. The authoritative number is __FreeBSD_version in opnsense/src
# stable/<series> sys/sys/param.h; a base never moves within a series, so
# this pin moves only when a new series lands (roughly January and July).
# Deliberately NO renovate marker: FreeBSD point releases must not bump this.
# Generated by `ci/freebsd-pin.sh <release> opnsense` -- edit that, not this.
RELEASE=14.3
IMAGE_URL_AMD64=https://archive.freebsd.org/old-releases/VM-IMAGES/14.3-RELEASE/amd64/Latest/FreeBSD-14.3-RELEASE-amd64-BASIC-CLOUDINIT-ufs.qcow2.xz
IMAGE_URL_ARM64=https://archive.freebsd.org/old-releases/VM-IMAGES/14.3-RELEASE/aarch64/Latest/FreeBSD-14.3-RELEASE-arm64-aarch64-BASIC-CLOUDINIT-ufs.qcow2.xz
IMAGE_SHA512_AMD64=d9d59ded03ef3431decc5e402b1cfcd2735e88604c42bc87137f0d7334100ba0b35cf3916fac85dc0458d17487b3e8183d0a91be2d20d617c30a3957aa70eb4b
IMAGE_SHA512_ARM64=84aa3980c3a1a5569ca83dd447d25ab5a1a1293397346384128570489fcf08c57488ca8af558bb8eb0d0a25f3eadb21642ab75443c2a8b62a8b076dd369c8a65
BASE_URL_AMD64=https://archive.freebsd.org/old-releases/amd64/14.3-RELEASE/base.txz
BASE_URL_ARM64=https://archive.freebsd.org/old-releases/arm64/aarch64/14.3-RELEASE/base.txz
BASE_SHA256_AMD64=e38b5cf756d60086a6c2f736eff19cc7685f7e2313e31d14342fc8df57200a92
BASE_SHA256_ARM64=f83e824cb7a20dbadb2888a8bd253e6a1ac35024cc6dc8af9f3e229f75ec7129
