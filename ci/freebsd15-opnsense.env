# Pinned FreeBSD release for the publish pipeline's daemon-package build VMs:
# the FreeBSD BASE of the OPNsense series this major serves (the series is
# named by the publish workflows' matrices), NOT the newest 15.x point
# release. pkg stamps every package it creates with the build host's
# __FreeBSD_version, and pkg on the target rejects anything newer than its
# own userland, so the daemon must be built on the base its OPNsense target
# runs. The authoritative number is __FreeBSD_version in opnsense/src
# stable/<series> sys/sys/param.h; a base never moves within a series, so
# this pin moves only when a new series lands (roughly January and July).
# Deliberately NO renovate marker: FreeBSD point releases must not bump this.
# Generated by `ci/freebsd-pin.sh <release> opnsense` -- edit that, not this.
RELEASE=15.1
IMAGE_URL_AMD64=https://archive.freebsd.org/old-releases/VM-IMAGES/15.1-RELEASE/amd64/Latest/FreeBSD-15.1-RELEASE-amd64-BASIC-CLOUDINIT-ufs.qcow2.xz
IMAGE_URL_ARM64=https://archive.freebsd.org/old-releases/VM-IMAGES/15.1-RELEASE/aarch64/Latest/FreeBSD-15.1-RELEASE-arm64-aarch64-BASIC-CLOUDINIT-ufs.qcow2.xz
IMAGE_SHA512_AMD64=bea82b91a983a20eb7ecdc6f11c1006d394ebbc5668b7f0ecdecf5a54fc5f9ea3a8384fc9f74a6611a9b08c183fa953327147ebfaa0de199fd015e67dd1e608a
IMAGE_SHA512_ARM64=df37de822fffdf19d7dddfdd3947390b5ae4dcd7f18a5ef6c6e6bb22effd52b593e09970c85ae081a2bb8290bad99a0fa5e473aed41be332a47bbac62c62bfb4
BASE_URL_AMD64=https://archive.freebsd.org/old-releases/amd64/15.1-RELEASE/base.txz
BASE_URL_ARM64=https://archive.freebsd.org/old-releases/arm64/aarch64/15.1-RELEASE/base.txz
BASE_SHA256_AMD64=3768988b151c20f965679062b065c63a977d6bbb9f47fd83695ec2c40790c18f
BASE_SHA256_ARM64=5b7a46a0abfbe23a1d4454b5600e2efcfce16b705bc7e9c851c37470c035ef98
